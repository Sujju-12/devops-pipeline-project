apiVersion: v1
kind: Service
metadata:
  name: employee-api-service
  namespace: devops
  labels:
    app: employee-api
spec:
  type: NodePort
  selector:
    app: employee-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
